# AI Web Agent

Автономный Web-Агент на Node.js/TypeScript с использованием Playwright и Anthropic SDK для многошаговой веб-автоматизации.

## Особенности

- **ReAct цикл**: Thought-Action-Observation для интеллектуальной автоматизации
- **Multi-tab поддержка**: Управление несколькими вкладками браузера
- **Security Layer**: LLM-оценка рисков перед выполнением действий
- **Интерактивный CLI**: Human-in-the-loop с confirmations
- **Элемент detection**: Умный поиск и инъекция data-agent-id
- **Memory & Learning**: Сохранение и анализ истории действий

## Архитектура

```
src/
├── agent/           # ReAct агент с памятью и обучением
├── automation/      # Playwright автоматизация
├── security/        # Оценка рисков и безопасность
├── cli/            # Интерактивный интерфейс
├── utils/          # Утилиты и конфигурация
└── types/          # TypeScript типы
```

## Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd AIbrowser

# Установка зависимостей
npm install

# Сборка проекта
npm run build

# Установка Playwright браузеров
npx playwright install
```

## Конфигурация

Создайте `.env` файл на основе `.env.example`:

```bash
cp .env.example .env
```

Обязательные переменные:
- `ANTHROPIC_API_KEY`: API ключ Anthropic Claude
- `USER_DATA_DIR`: Директория для данных браузера
- `LOG_LEVEL`: OFF/INFO/DEBUG
- `ENABLE_RISK_EVALUATION`: true/false

## Использование

### Интерактивный режим

```bash
npm start interactive
```

### Прямое выполнение задачи

```bash
npm start execute "Открыть Google и найти информацию о TypeScript"
```

### С опциями

```bash
npm start execute "Задача" --max-iterations 30 --headless
```

## CLI команды

- `interactive` - Интерактивный режим с меню
- `execute <goal>` - Выполнить задачу напрямую
- `config` - Показать текущую конфигурацию
- `test` - Запустить базовые тесты

## Action Tools

Агент поддерживает следующие действия:

1. **click_element(id)** - Кликнуть по элементу
2. **type_text(id, text)** - Ввести текст в поле
3. **navigate_to(url)** - Перейти по URL
4. **scroll_page(direction)** - Скроллить страницу
5. **switch_to_page(page_id)** - Переключить вкладку
6. **request_user_assistance(reason, critical)** - Запросить помощь

## Безопасность

### Risk Evaluation

Каждое действие проходит LLM-оценку рисков:
- **HIGH**: Финансовые операции, удаление данных
- **MEDIUM**: Навигация на новые домены
- **LOW**: Чтение, скроллинг, навигация в пределах домена

### User Confirmation

High-risk действия требуют подтверждения пользователя через CLI.

## Примеры использования

```bash
# Поиск информации
npm start exec "Найти на Wikipedia статью об искусственном интеллекте"

# Заполнение форм
npm start exec "Заполнить контактную форму на сайте example.com"

# Multi-tab задачи
npm start exec "Открыть новостной сайт и открыть 3 статьи в новых вкладках"
```

## Разработка

```bash
# Режим разработки
npm run dev

# Сборка
npm run build

# Запуск тестов
npm test
```

## Структура ReAct цикла

1. **Thought**: Анализ текущего состояния и планирование
2. **Action**: Выполнение конкретного действия
3. **Observation**: Получение результата и обновление контекста
4. **Memory**: Сохранение опыта для обучения

## Логирование

Три уровня логирования:
- `OFF`: Только ошибки
- `INFO`: Ход выполнения, Thought/Action
- `DEBUG`: Полный контекст, элементы, LLM I/O

## Архитектурные компоненты

### WebAgent
Основной агент с ReAct циклом, интегрирующий все компоненты.

### BrowserManager
Управление браузером с поддержкой:
- Persistent context с userDataDir
- Multi-tab управление
- Page lifecycle

### ElementTagger
Интеллектуальный поиск элементов:
- Playwright getByRole() оптимизация
- Инъекция data-agent-id в DOM
- Фильтрация скрытых элементов

### RiskEvaluator
LLM-based оценка рисков:
- Анализ контекста действия
- Классификация уровней риска
- URL safety проверка

### AgentMemory
Система обучения:
- Хранение истории действий
- Поиск паттернов
- Контекстная ретривация

## Требования

- Node.js 18+
- TypeScript 5+
- Playwright браузеры
- Anthropic API доступ

## License

ISC

---

🤖 Generated with [Claude Code](https://claude.com/claude-code)